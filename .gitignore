node_modules/
dist/
.astro/
.wrangler/
.env
.env.*
.DS_Store
*.log

# Agent scratch worktrees — a nested worktree here once showed up as
# untracked repo content (an agent created it inside the canonical clone).
.claude/worktrees/
